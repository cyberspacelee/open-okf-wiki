---
coverage: full
sources: []
---

## 定义与所有权

用领域语言定义该 Concept，说明所属 Domain 和 owner，并与相近术语区分。

## 模型依据

说明持久化依据是 `opengauss`、`code` 还是 `none`。链接归属的 DataModel 和生成的 Table 参考页，不复制其结构。

## 不变量与规则

用紧凑表格列出语义规则、行为入口和可观察到的失败。工作流细节放在链接的 Flow、Procedure 或 Lifecycle 页面。

## 关系与变更入口

描述有证据支持的语义关系，并把常见变更路由到 owner、模型和测试。不要把逻辑关系写成物理约束。
