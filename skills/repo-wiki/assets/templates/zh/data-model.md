---
coverage: full
sources: []
---

## 模型依据

说明各 Concept 使用 `opengauss` 还是 `code`、覆盖是否完整，以及 Catalog 选择缺口。OpenGauss 决定物理结构，代码证据只决定逻辑投影和行为。

## 物理模型

<!-- okf-generated:model -->

## 逻辑关系

代码恢复模型在这里呈现规划的逻辑 ER 图。OpenGauss-backed 模型只描述不属于物理约束且有证据支持的语义关系，并与生成视图分开。

## 所有权与边界

说明每个选定实体归属哪个来源，以及代码或服务边界在哪里与持久化相交。

## 参考模型

链接生成的 Schema 和 Table 参考页。字段、约束和索引清单只保留在这些页面中。

## 代码与数据映射

用紧凑表格描述读写路径、生命周期事件和所属代码概念。把缺失证据记录为缺口。
