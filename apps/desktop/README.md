# Desktop shell (later phase)

Placeholder for a **Tauri** (preferred) or **Electron** wrapper around:

1. Local `@okf-wiki/server` on `127.0.0.1`
2. `@okf-wiki/web` UI in a WebView
3. Optional native directory picker for Workspace / Git source paths

Do not implement packaging until the Web + server MVP is usable in a browser.
