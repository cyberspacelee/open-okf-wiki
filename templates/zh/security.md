---
type: Security Model
scope: repo
optional: true
instructions: >-
  仅当认证、授权或数据权限有源码证据时生成（如 Spring Security 配置、
  网关过滤器、注解式权限、租户隔离逻辑）。覆盖：认证链路（token 从哪来、
  在哪校验、失败行为）、角色与权限模型（角色定义处、权限点、校验位置）、
  数据权限（行级/租户过滤的实现位置）。写"哪些路径不受保护"时必须有
  放行配置的源码证据。这是防护结构文档，不是渗透指南；不要虚构威胁模型。
  隐式单仓写在 Wiki 根；显式多仓写在 <scopeId>/。
---

# {{title}}

{{description}}

## 认证链路

## 角色与权限模型

## 数据权限

## 未防护面
